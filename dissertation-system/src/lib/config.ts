export const siteConfig = {
  institution: process.env.NEXT_PUBLIC_INSTITUTION_NAME ?? 'University',
  department: process.env.NEXT_PUBLIC_DEPARTMENT_NAME ?? 'Department',
  appName: process.env.NEXT_PUBLIC_APP_NAME ?? 'DRS',
}
